DATABASE_URL=sqlite:///./app.db
JWT_SECRET_KEY=change-me
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440
# "development" (default) or "production" -- production sends the auth
# cookie with Secure set, requiring HTTPS. Set to "production" on the
# deployed host.
ENVIRONMENT=development
